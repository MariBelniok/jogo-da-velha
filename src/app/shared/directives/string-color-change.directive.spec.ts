import { StringColorChangeDirective } from './string-color-change.directive';

describe('StringColorChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new StringColorChangeDirective();
    expect(directive).toBeTruthy();
  });
});
